project:
    name: Manage Package
    package:
        name:  Manage Package
        namespace: devsfdx
        api_version: '42'
    dependencies:
        - github: 'https://github.com/a4ankit/ManagePackage_Test'

    git: 
        prefix_feature: 'sf' 
        prefix_beta: 'sf' 
        prefix_release: 'sf'
    test:
        name_match: 'test'

orgs:
    scratch:
        dev_namespaced:
            config_file: orgs/dev.json
            days: 7
            namespaced: True
